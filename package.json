{
  "name": "qingsiyuan",
  "private": true,
  "scripts": {
    "dev": "sh -c \"(cd server && npm run dev) & (cd frontend && npm run dev) & wait\"",
    "build": "sh -c \"cd server && npm run build && cd ../frontend && npm run build\"",
    "lint": "sh -c \"cd server && npm run lint && cd ../frontend && npm run lint\"",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^9.1.6"
  }
}


